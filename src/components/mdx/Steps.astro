---
/**
 * Bọc một danh sách có số trong Markdown để nó hiện thành các bước có đường nối.
 *
 * Cách dùng trong MDX — bên trong vẫn là Markdown thuần, không phải thẻ lạ:
 *
 * <Steps>
 * 1. **Bước đầu.** Mô tả.
 * 2. **Bước hai.** Mô tả.
 * </Steps>
 */
---

<div class="steps">
  <slot />
</div>
